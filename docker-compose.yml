version: "3.8"

services:
  web:
    image: mahrshi336/video-tutorial-docker:latest
    ports:
      - "8081:80"
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
      update_config:
        parallelism: 1
        delay: 5s
        order: start-first
